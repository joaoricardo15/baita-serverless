service: baita-help

plugins:
  - serverless-offline
  - serverless-plugin-webpack
  - serverless-deployment-bucket

custom:
  webpack:
    config: ./webpack.config.js
  serverless-offline:
    useChildProcesses: true

provider:
  name: aws
  timeout: 10
  region: us-east-1
  memorySize: 512
  role: serviceRole
  runtime: nodejs12.x
  versionFunctions: false
  stage: ${opt:stage, 'dev'}
  apiName: ${self:provider.environment.SERVICE_API}
  deploymentBucket:
    name: ${self:provider.environment.SERVICE_BUCKET}
    blockPublicAccess: true
  environment:
    SERVICE_PREFIX: ${self:service}-${self:provider.stage}
    SERVICE_API: ${self:provider.environment.SERVICE_PREFIX}-serverless
    SERVICE_ROLE: ${self:provider.environment.SERVICE_PREFIX}-serverless
    SERVICE_DEV_URL: https://1ainyn2f42.execute-api.us-east-1.amazonaws.com/dev
    SERVICE_PROD_URL: https://1yp22xgyt1.execute-api.us-east-1.amazonaws.com/prod

    # Bot
    BOT_PREFIX: ${self:service}-prod-bot

    # Database
    APPS_TABLE: ${self:service}-prod-apps
    BOTS_TABLE: ${self:service}-prod-bots
    LOGS_TABLE: ${self:service}-prod-logs
    USERS_TABLE: ${self:service}-prod-users
    CONNECTIONS_TABLE: ${self:service}-prod-connections

    # Buckets
    BOTS_BUCKET: ${self:provider.environment.SERVICE_PREFIX}-bots
    SERVICE_BUCKET: ${self:provider.environment.SERVICE_PREFIX}-serverless

    # Iam
    BOTS_ROLE: ${self:provider.environment.BOT_PREFIX}
    BOTS_POLICY: ${self:provider.environment.BOT_PREFIX}
    BOTS_PERMISSION: arn:aws:iam::514311354050:role/${self:provider.environment.BOT_PREFIX}

    # Auth0
    AUTH0_AUDIENCE: ${file(./src/assets/partners/auth0/secrets.json):audience}
    AUTH0_CLIENT_PUBLIC_KEY: ${file(./src/assets/partners/auth0/public_key)}
    
    # Pipedrive
    PIPEDRIVE_AUTH_URL: ${file(./src/assets/partners/pipedrive/secrets.json):auth_uri}
    PIPEDRIVE_CLIENT_ID: ${file(./src/assets/partners/pipedrive/secrets.json):client_id}
    PIPEDRIVE_CLIENT_SECRET: ${file(./src/assets/partners/pipedrive/secrets.json):client_secret}
    
    # Wix
    WIX_AUTH_URL: ${file(./src/assets/partners/wix/secrets.json):auth_uri}
    WIX_CLIENT_ID: ${file(./src/assets/partners/wix/secrets.json):client_id}
    WIX_CLIENT_SECRET: ${file(./src/assets/partners/wix/secrets.json):client_secret}

    # RD Station
    RDSTATION_AUTH_URL: ${file(./src/assets/partners/rdstation/secrets.json):auth_uri}
    RDSTATION_CLIENT_ID: ${file(./src/assets/partners/rdstation/secrets.json):client_id}
    RDSTATION_CLIENT_SECRET: ${file(./src/assets/partners/rdstation/secrets.json):client_secret}

    # Google
    GOOGLE_AUTH_URL: ${file(./src/assets/partners/google/secrets.json):web.token_uri}
    GOOGLE_CLIENT_ID: ${file(./src/assets/partners/google/secrets.json):web.client_id}
    GOOGLE_CLIENT_SECRET: ${file(./src/assets/partners/google/secrets.json):web.client_secret}

    # Franq
    FRANQ_AUTH_URL: ${file(./src/assets/partners/franq/secrets.json):auth_uri}
    FRANQ_CLIENT_ID: ${file(./src/assets/partners/franq/secrets.json):client_id}
    FRANQ_CLIENT_SECRET: ${file(./src/assets/partners/franq/secrets.json):client_secret}

functions:
  ### internal services ### 
  auth:
    handler: src/services/auth/auth.handler
  log-create:
    handler: src/services/log-create/log-create.handler
  sample-update:
    handler: src/services/sample-update/sample-update.handler
  
  ### custom bot services ### 
  http-request:
    handler: src/services/http-request/http-request.handler
  oauth2-request:
    handler: src/services/oauth2-request/oauth2-request.handler
  code-execute:
    handler: src/services/code-execute/code-execute.handler
  pipedrive-backup:
    handler: src/services/pipedrive/backup/backup.handler

  ### user endpoints ### 
  user-create:
    handler: src/endpoints/user-create/user-create.handler
    events:
      - http:
          path: user/create
          method: post
  user-bots:
    handler: src/endpoints/user-bots/user-bots.handler
    events:
      - http:
          path: user/{user_id}/bots
          method: get
          cors: true
          # authorizer:
          #   name: auth
  user-bot:
    handler: src/endpoints/user-bot/user-bot.handler
    events:
      - http:
          path: user/{user_id}/bot/{bot_id}
          method: get
          cors: true
          # authorizer:
          #   name: auth
  user-connections:
    handler: src/endpoints/user-connections/user-connections.handler
    events:
      - http:
          path: user/{user_id}/connections
          method: get
          cors: true
          # authorizer:
          #   name: auth
  
  ### bot endpoints ### 
  bot-create:
    handler: src/endpoints/bot-create/bot-create.handler
    events:
      - http:
          path: bot/create
          method: post
          cors: true
          # authorizer:
          #   name: auth
  bot-update:
    handler: src/endpoints/bot-update/bot-update.handler
    events:
      - http:
          path: bot/update
          method: post
          cors: true
          # authorizer:
          #   name: auth
  bot-delete:
    handler: src/endpoints/bot-delete/bot-delete.handler
    events:
      - http:
          path: bot/delete
          method: post
          cors: true
          # authorizer:
          #   name: auth
  bot-deploy:
    handler: src/endpoints/bot-deploy/bot-deploy.handler
    events:
      - http:
          path: bot/deploy
          method: post
          cors: true
          # authorizer:
          #   name: auth
  bot-test:
    handler: src/endpoints/bot-test/bot-test.handler
    events:
      - http:
          path: bot/test
          method: post
          cors: true
          # authorizer:
          #   name: auth
  bot-logs:
    handler: src/endpoints/bot-logs/bot-logs.handler
    events:
      - http:
          path: bot/{bot_id}/logs
          method: get
          cors: true
          # authorizer:
          #   name: auth
  bot-usage:
    handler: src/endpoints/bot-usage/bot-usage.handler
    memorySize: 3000
    events:
      - http:
          path: bot/{bot_id}/usage
          method: get
          cors: true
          # authorizer:
          #   name: auth

    ### connectors ### 
  
  connectors-pipedrive:
    handler: src/connectors/pipedrive/pipedrive.handler
    events:
      - http:
          path: connectors/pipedrive
          method: get
  connectors-google:
    handler: src/connectors/google/google.handler
    events:
      - http:
          path: connectors/google
          method: get
  connectors-rdstation:
    handler: src/connectors/rdstation/rdstation.handler
    events:
      - http:
          path: connectors/rdstation
          method: get
  connectors-wix:
    handler: src/connectors/wix/wix.handler
    events:
      - http:
          path: connectors/wix
          method: get
  connectors-franq:
    handler: src/connectors/franq/franq.handler
    events:
      - http:
          path: connectors/franq
          method: get

  ### webhooks ### 
  triggers-baitahelp:
    handler: src/triggers/baitahelp/baitahelp.handler
    events:
      - http:
          path: triggers/baitahelp
          method: get
  triggers-pipedrive:
    handler: src/triggers/pipedrive/pipedrive.handler
    events:
      - http:
          path: triggers/pipedrive
          method: get
  triggers-google:
    handler: src/triggers/google/google.handler
    events:
      - http:
          path: triggers/google
          method: get
  triggers-wix:
    handler: src/triggers/wix/wix.handler
    events:
      - http:
          path: triggers/wix
          method: get

resources:
  Conditions:
      IsProd:
        Fn::Equals:
          - ${self:provider.stage}
          - prod 
  Resources:
    # Database 
    appsTable:
      Condition: IsProd
      Type: AWS::DynamoDB::Table
      DeletionPolicy: Retain
      Properties:
        TableName: ${self:provider.environment.APPS_TABLE}
        AttributeDefinitions:
          - AttributeName: app_id
            AttributeType: S
        KeySchema:
          - AttributeName: app_id
            KeyType: HASH
        ProvisionedThroughput:
          ReadCapacityUnits: 1
          WriteCapacityUnits: 1
    botsTable:
      Condition: IsProd
      Type: AWS::DynamoDB::Table
      DeletionPolicy: Retain
      Properties:
        TableName: ${self:provider.environment.BOTS_TABLE}
        AttributeDefinitions:
          - AttributeName: user_id
            AttributeType: S
          - AttributeName: bot_id
            AttributeType: S
        KeySchema:
          - AttributeName: user_id
            KeyType: HASH
          - AttributeName: bot_id
            KeyType: RANGE
        ProvisionedThroughput:
          ReadCapacityUnits: 1
          WriteCapacityUnits: 1
    logsTable:
      Condition: IsProd
      Type: AWS::DynamoDB::Table
      DeletionPolicy: Retain
      Properties:
        TableName: ${self:provider.environment.LOGS_TABLE}
        AttributeDefinitions:
          - AttributeName: bot_id
            AttributeType: S
          - AttributeName: timestamp
            AttributeType: N
        KeySchema:
          - AttributeName: bot_id
            KeyType: HASH
          - AttributeName: timestamp
            KeyType: RANGE
        ProvisionedThroughput:
          ReadCapacityUnits: 1
          WriteCapacityUnits: 1
    usersTable:
      Condition: IsProd
      Type: AWS::DynamoDB::Table
      DeletionPolicy: Retain
      Properties:
        TableName: ${self:provider.environment.USERS_TABLE}
        AttributeDefinitions:
          - AttributeName: user_id
            AttributeType: S
        KeySchema:
          - AttributeName: user_id
            KeyType: HASH
        ProvisionedThroughput:
          ReadCapacityUnits: 1
          WriteCapacityUnits: 1
    connectionsTable:
      Condition: IsProd
      Type: AWS::DynamoDB::Table
      DeletionPolicy: Retain
      Properties:
        TableName: ${self:provider.environment.CONNECTIONS_TABLE}
        AttributeDefinitions:
          - AttributeName: user_id
            AttributeType: S
          - AttributeName: connection_id
            AttributeType: S
        KeySchema:
          - AttributeName: user_id
            KeyType: HASH
          - AttributeName: connection_id
            KeyType: RANGE
        ProvisionedThroughput:
          ReadCapacityUnits: 1
          WriteCapacityUnits: 1

    # Butckets
    botsBucket:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: ${self:provider.environment.BOTS_BUCKET}
    
    # Iam
    serviceRole:
      Type: AWS::IAM::Role
      Properties:
        RoleName: ${self:provider.environment.SERVICE_ROLE}
        AssumeRolePolicyDocument:
          Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: sts:AssumeRole
              Principal:
                Service:
                  - lambda.amazonaws.com
        Policies:
          - PolicyName: ${self:provider.environment.BOTS_POLICY}
            PolicyDocument:
              Version: '2012-10-17'
              Statement:
                - Effect: Allow
                  Action:
                    - 's3:*'
                    - 'logs:*'
                    - 'lambda:*'
                    - 'dynamodb:*'
                    - 'apigateway:*'
                    - 'iam:PassRole'
                  Resource: '*' 
    botsRole:
      Type: AWS::IAM::Role
      Properties:
        RoleName: ${self:provider.environment.BOTS_ROLE}
        AssumeRolePolicyDocument:
          Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: sts:AssumeRole
              Principal:
                Service:
                  - events.amazonaws.com
                  - lambda.amazonaws.com
                  - apigateway.amazonaws.com
        Policies:
          - PolicyName: ${self:provider.environment.BOTS_POLICY}
            PolicyDocument:
              Version: '2012-10-17'
              Statement:
                - Effect: Allow
                  Action:
                    - lambda:InvokeFunction
                  Resource: '*'
